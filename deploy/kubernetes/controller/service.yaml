apiVersion: v1
kind: Service
metadata:
  name: csi-driver-truenas-csi-controller-metrics
  namespace: kube-system
  labels:
    app: csi-driver-truenas-csi
spec:
  selector:
    app: csi-driver-truenas-csi-controller
  ports:
    - port: 9189
      name: metrics
      targetPort: metrics
